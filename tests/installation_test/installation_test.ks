import cast_ons;
import stockinette;
with Carrier as 1, width as 6, height as 4:{
    cast_ons.alt_tuck_cast_on(width);
    for row in range(0, height):{
        in reverse direction:{
            knit Loops;
        }
    }
}